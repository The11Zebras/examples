# SEIS660 basic Vagrant file
# This Vagrantfile has all the comments removed from the original.


Vagrant.configure(2) do |config|
  config.vm.box = "precise64"
  config.vm.provision             :shell, path: "./guru9746-Lab02.sh"
end
